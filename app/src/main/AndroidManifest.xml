<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="epi.pfa.medicalcenter">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".ServicesActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ServiceActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DoctorActivity"
            android:theme="@style/AppTheme.Dark.ActionBar" />
        <activity
            android:name=".ProfileActivity"
            android:theme="@style/AppTheme.Dark.ActionBar" />
        <activity android:name=".ConsultationsActivity" />
        <activity
            android:name=".ContactActivity"
            android:theme="@style/AppTheme.Dark.ActionBar" />
        <activity
            android:name=".AboutActivity"
            android:theme="@style/AppTheme.Dark.ActionBar" />
        <activity android:name=".myDoctorsActivity" />
        <activity android:name=".TreatmentsActivity" />
        <activity android:name=".ShowConsultationActivity" />
        <activity android:name=".howTreatmentActivity"></activity>
    </application>

</manifest>